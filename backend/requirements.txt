Flask==3.0.3
Flask-Cors==5.0.0
numpy==1.26.4
phevaluator==0.5.3.1
PyPokerEngine==1.0.1

# Production WSGI servers (the dev server in strategy_api.py is local-only):
#   gunicorn -> Linux deploy box; waitress -> cross-platform (incl. Windows).
gunicorn==23.0.0
waitress==3.0.2

# DynamoDB session + leaderboard stores (ALLIN_SESSION_STORE / ALLIN_STORE_BACKEND
# = dynamodb). Optional for local dev, which defaults to the in-memory stores.
boto3==1.35.76

# Google sign-in via Cognito: ID-token (RS256/JWKS) validation in backend/api/auth.py.
PyJWT==2.9.0
cryptography==43.0.1

# Handle profanity filter (player names). Optional at runtime — player_store falls
# back to a small built-in blocklist if it's absent — but pinned for prod.
better-profanity==0.7.0
